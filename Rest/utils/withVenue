module.exports = (fields, DataTypes) => ({
  ...fields,
  venueId: {
    type: DataTypes.INTEGER,
    allowNull: false,
    references: {
      model: 'Venues', // Refers to the Venues table
      key: 'venueId',
    },
    onUpdate: 'CASCADE',
    onDelete: 'CASCADE',
  },
});
